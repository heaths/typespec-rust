// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::{
    lists::clients::DocumentationListsClient,
    text_formatting::clients::DocumentationTextFormattingClient,
};
use azure_core::{
    fmt::SafeDebug,
    http::{ClientOptions, Pipeline, Url},
    tracing, Result,
};

/// Illustrates documentation generation and formatting features
#[tracing::client]
pub struct DocumentationClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`DocumentationClient`](DocumentationClient)
#[derive(Clone, Default, SafeDebug)]
pub struct DocumentationClientOptions {
    /// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl DocumentationClient {
    /// Creates a new DocumentationClient requiring no authentication.
    ///
    /// # Arguments
    ///
    /// * `endpoint` - Service host
    /// * `options` - Optional configuration for the client.
    #[tracing::new("Documentation")]
    pub fn with_no_credential(
        endpoint: &str,
        options: Option<DocumentationClientOptions>,
    ) -> Result<Self> {
        let options = options.unwrap_or_default();
        let endpoint = Url::parse(endpoint)?;
        if !endpoint.scheme().starts_with("http") {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                format!("{endpoint} must use http(s)"),
            ));
        }
        Ok(Self {
            endpoint,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                Vec::default(),
                None,
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Returns a new instance of DocumentationListsClient.
    #[tracing::subclient]
    pub fn get_documentation_lists_client(&self) -> DocumentationListsClient {
        DocumentationListsClient {
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
        }
    }

    /// Returns a new instance of DocumentationTextFormattingClient.
    #[tracing::subclient]
    pub fn get_documentation_text_formatting_client(&self) -> DocumentationTextFormattingClient {
        DocumentationTextFormattingClient {
            endpoint: self.endpoint.clone(),
            pipeline: self.pipeline.clone(),
        }
    }
}
