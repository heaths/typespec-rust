// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::lists::generated::models::{
    BulletPointsModel, BulletPointsModelRequest, DocumentationListsClientBulletPointsModelOptions,
    DocumentationListsClientBulletPointsOpOptions, DocumentationListsClientNumberedOptions,
};
use azure_core::{
    error::CheckSuccessOptions,
    http::{
        Method, NoFormat, Pipeline, PipelineSendOptions, Request, RequestContent, Response, Url,
        UrlExt,
    },
    tracing, Result,
};

#[tracing::client]
pub struct DocumentationListsClient {
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

impl DocumentationListsClient {
    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Documentation.Lists.bulletPointsModel")]
    pub async fn bullet_points_model(
        &self,
        input: BulletPointsModel,
        options: Option<DocumentationListsClientBulletPointsModelOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/documentation/lists/bullet-points/model");
        let mut request = Request::new(url, Method::Post);
        request.insert_header("content-type", "application/json");
        let body: RequestContent<BulletPointsModelRequest> =
            BulletPointsModelRequest { input }.try_into()?;
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// This tests:
    /// - Simple bullet point. This bullet point is going to be very long to test how text wrapping is handled in bullet points
    ///   within documentation comments. It should properly indent the wrapped lines.
    /// - Another bullet point with **bold text**. This bullet point is also intentionally long to see how the formatting is preserved
    ///   when the text wraps onto multiple lines in the generated documentation.
    /// - Third bullet point with *italic text*. Similar to the previous points, this one is extended to ensure that the wrapping
    ///   and formatting are correctly applied in the output.
    /// - Complex bullet point with **bold** and *italic* combined. This bullet point combines both bold and italic formatting
    ///   and is long enough to test the wrapping behavior in such cases.
    /// - **Bold bullet point**: A bullet point that is entirely bolded. This point is also made lengthy to observe how the bold
    ///   formatting is maintained across wrapped lines.
    /// - *Italic bullet point*: A bullet point that is entirely italicized. This final point is extended to verify that italic
    ///   formatting is correctly applied even when the text spans multiple lines.
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Documentation.Lists.bulletPointsOp")]
    pub async fn bullet_points_op(
        &self,
        options: Option<DocumentationListsClientBulletPointsOpOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/documentation/lists/bullet-points/op");
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Steps to follow:
    /// 1. First step with **important** note
    /// 2. Second step with *emphasis*
    /// 3. Third step combining **bold** and *italic*
    /// 4. **Final step**: Review all steps for *accuracy*.
    ///
    /// # Arguments
    ///
    /// * `options` - Optional parameters for the request.
    #[tracing::function("Documentation.Lists.numbered")]
    pub async fn numbered(
        &self,
        options: Option<DocumentationListsClientNumberedOptions<'_>>,
    ) -> Result<Response<(), NoFormat>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        url.append_path("/documentation/lists/numbered");
        let mut request = Request::new(url, Method::Get);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[204],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}
