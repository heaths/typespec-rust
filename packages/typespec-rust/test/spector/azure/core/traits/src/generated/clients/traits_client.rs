// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT License.
//
// Code generated by Microsoft (R) Rust Code Generator. DO NOT EDIT.

use crate::generated::models::{
    TraitsClientRepeatableActionOptions, TraitsClientSmokeTestOptions, User, UserActionParam,
    UserActionResponse,
};
use azure_core::{
    error::CheckSuccessOptions,
    fmt::SafeDebug,
    http::{
        ClientOptions, Method, Pipeline, PipelineSendOptions, Request, RequestContent, Response,
        Url, UrlExt,
    },
    time::to_rfc7231,
    tracing, Result,
};

/// Illustrates Azure Core operation customizations by traits
#[tracing::client]
pub struct TraitsClient {
    pub(crate) api_version: String,
    pub(crate) endpoint: Url,
    pub(crate) pipeline: Pipeline,
}

/// Options used when creating a [`TraitsClient`](TraitsClient)
#[derive(Clone, SafeDebug)]
pub struct TraitsClientOptions {
    /// The API version to use for this operation.
    pub api_version: String,
    /// Allows customization of the client.
    pub client_options: ClientOptions,
}

impl TraitsClient {
    /// Creates a new TraitsClient requiring no authentication.
    ///
    /// # Arguments
    ///
    /// * `endpoint` - Service host
    /// * `options` - Optional configuration for the client.
    #[tracing::new("_Specs_.Azure.Core.Traits")]
    pub fn with_no_credential(
        endpoint: &str,
        options: Option<TraitsClientOptions>,
    ) -> Result<Self> {
        let options = options.unwrap_or_default();
        let endpoint = Url::parse(endpoint)?;
        if !endpoint.scheme().starts_with("http") {
            return Err(azure_core::Error::with_message(
                azure_core::error::ErrorKind::Other,
                format!("{endpoint} must use http(s)"),
            ));
        }
        Ok(Self {
            endpoint,
            api_version: options.api_version,
            pipeline: Pipeline::new(
                option_env!("CARGO_PKG_NAME"),
                option_env!("CARGO_PKG_VERSION"),
                options.client_options,
                Vec::default(),
                Vec::default(),
                None,
            ),
        })
    }

    /// Returns the Url associated with this client.
    pub fn endpoint(&self) -> &Url {
        &self.endpoint
    }

    /// Test for repeatable requests
    ///
    /// # Arguments
    ///
    /// * `id` - The user's id.
    /// * `body` - The body parameter.
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`UserActionResponseHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use spector_coretraits::models::{UserActionResponse, UserActionResponseHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<UserActionResponse> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(repeatability_result) = response.repeatability_result()? {
    ///         println!("repeatability-result: {:?}", repeatability_result);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`repeatability_result`()](crate::generated::models::UserActionResponseHeaders::repeatability_result) - repeatability-result
    ///
    /// [`UserActionResponseHeaders`]: crate::generated::models::UserActionResponseHeaders
    #[tracing::function("_Specs_.Azure.Core.Traits.repeatableAction")]
    pub async fn repeatable_action(
        &self,
        id: i32,
        body: RequestContent<UserActionParam>,
        options: Option<TraitsClientRepeatableActionOptions<'_>>,
    ) -> Result<Response<UserActionResponse>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/azure/core/traits/user/{id}:repeatableAction");
        path = path.replace("{id}", &id.to_string());
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Post);
        request.insert_header("accept", "application/json");
        request.insert_header("content-type", "application/json");
        if let Some(repeatability_first_sent) = options.repeatability_first_sent {
            request.insert_header(
                "repeatability-first-sent",
                to_rfc7231(&repeatability_first_sent),
            );
        }
        if let Some(repeatability_request_id) = options.repeatability_request_id.as_ref() {
            request.insert_header("repeatability-request-id", repeatability_request_id);
        }
        request.set_body(body);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }

    /// Get a resource, sending and receiving headers.
    ///
    /// # Arguments
    ///
    /// * `id` - The user's id.
    /// * `foo` - header in request
    /// * `options` - Optional parameters for the request.
    ///
    /// ## Response Headers
    ///
    /// The returned [`Response`](azure_core::http::Response) implements the [`UserHeaders`] trait, which provides
    /// access to response headers. For example:
    ///
    /// ```no_run
    /// use azure_core::{Result, http::Response};
    /// use spector_coretraits::models::{User, UserHeaders};
    /// async fn example() -> Result<()> {
    ///     let response: Response<User> = unimplemented!();
    ///     // Access response headers
    ///     if let Some(bar) = response.bar()? {
    ///         println!("bar: {:?}", bar);
    ///     }
    ///     if let Some(etag_header) = response.etag_header()? {
    ///         println!("etag: {:?}", etag_header);
    ///     }
    ///     if let Some(client_request_id) = response.client_request_id()? {
    ///         println!("x-ms-client-request-id: {:?}", client_request_id);
    ///     }
    ///     Ok(())
    /// }
    /// ```
    ///
    /// ### Available headers
    /// * [`bar`()](crate::generated::models::UserHeaders::bar) - bar
    /// * [`etag_header`()](crate::generated::models::UserHeaders::etag_header) - etag
    /// * [`client_request_id`()](crate::generated::models::UserHeaders::client_request_id) - x-ms-client-request-id
    ///
    /// [`UserHeaders`]: crate::generated::models::UserHeaders
    #[tracing::function("_Specs_.Azure.Core.Traits.smokeTest")]
    pub async fn smoke_test(
        &self,
        id: i32,
        foo: String,
        options: Option<TraitsClientSmokeTestOptions<'_>>,
    ) -> Result<Response<User>> {
        let options = options.unwrap_or_default();
        let ctx = options.method_options.context.to_borrowed();
        let mut url = self.endpoint.clone();
        let mut path = String::from("/azure/core/traits/user/{id}");
        path = path.replace("{id}", &id.to_string());
        url.append_path(&path);
        let mut query_builder = url.query_builder();
        query_builder.set_pair("api-version", &self.api_version);
        query_builder.build();
        let mut request = Request::new(url, Method::Get);
        request.insert_header("accept", "application/json");
        if let Some(if_match) = options.if_match.as_ref() {
            request.insert_header("if-match", if_match);
        }
        if let Some(if_modified_since) = options.if_modified_since {
            request.insert_header("if-modified-since", to_rfc7231(&if_modified_since));
        }
        if let Some(if_none_match) = options.if_none_match.as_ref() {
            request.insert_header("if-none-match", if_none_match);
        }
        if let Some(if_unmodified_since) = options.if_unmodified_since {
            request.insert_header("if-unmodified-since", to_rfc7231(&if_unmodified_since));
        }
        request.insert_header("foo", foo);
        let rsp = self
            .pipeline
            .send(
                &ctx,
                &mut request,
                Some(PipelineSendOptions {
                    check_success: CheckSuccessOptions {
                        success_codes: &[200],
                    },
                    ..Default::default()
                }),
            )
            .await?;
        Ok(rsp.into())
    }
}

/// Default value for [`TraitsClientOptions::api_version`].
pub(crate) const DEFAULT_API_VERSION: &str = "2022-12-01-preview";

impl Default for TraitsClientOptions {
    fn default() -> Self {
        Self {
            api_version: String::from(DEFAULT_API_VERSION),
            client_options: ClientOptions::default(),
        }
    }
}
